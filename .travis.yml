language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t simonnightingale/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true simonnightingale/docker-react npm run test -- --coverage